<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PersonDateRecyclerView" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:popupMenuStyle">@style/PopupMenu</item>
    </style>

    <style name="Theme.PersonDateRecyclerView" parent="Base.Theme.PersonDateRecyclerView" />

    <style name="PopupMenu" parent="android:Widget.Material.PopupMenu">
        <item name="android:popupElevation">.3dp</item>
        <item name="android:popupBackground">@drawable/shape_round_popup_menu</item>
        <item name="android:dropDownHorizontalOffset">8dp</item>
        <item name="android:dropDownVerticalOffset">-8dp</item>
    </style>
</resources>